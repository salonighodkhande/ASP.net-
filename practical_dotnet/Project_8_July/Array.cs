// Array - the collection of similar data type elements which are stored in contiguous memory locations. It is a fixed size data structure. It is used to store multiple values in a single variable, instead of declaring separate variables for each value. It is a reference type and can be single dimensional or multi-dimensional.
using System;

class Array{
    static void (){
        int[] rollno =[20, 21, 22, 23, 24];

        foreach(int r in rollno){
            Console.WriteLine(r);
        }
     
        
        
    }
}